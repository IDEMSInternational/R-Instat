library(instatCalculations)
library(databook)

# Todo: Remove these - are these set elsewhere

# Calculation labels
#These are the names of the list returned by apply_instat_calculation
c_data_label <- "data"
c_link_label <- "link"
c_has_summary_label <- "has_summary"
c_has_filter_label <- "has_filter"