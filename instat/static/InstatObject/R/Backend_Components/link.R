link <- R6Class("link",
                       public = list(
                         initialize = function() {
                         }
                       ),
                       private = list(
                       ),
                       active = list(
                       )
)
